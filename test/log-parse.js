// import test from 'ava';
// import assignTypes from '../lib/logger/assign-types';
// import parseLog from '../lib/parse-log';
//
// function logAndParse(t, i) {
// 	const input = i;
// 	const logged = assignTypes(input);
// 	const parsed = parseLog(logged);
// 	t.deepEqual([input], parsed);
// }
//
// test('log and parse string', t => logAndParse(t, 'A string'));
//
// test('log and parse number', t => logAndParse(t, 42));
//
// test('log and parse bool', t => logAndParse(t, true));
//
// test('log and parse null', t => logAndParse(t, null));
//
// test('log and parse undefined', t => logAndParse(t, undefined));
